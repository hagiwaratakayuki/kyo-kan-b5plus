const { Context } = require("../../../../kyo-kan/context");
const { ViewPatternController } = require("../../pattern/view/basic");

class TextController extends ViewPatternController {
    /**
     * 
     * @param {*} request 
     * @param {Context<any>} context 
     * @param {*} stateController 
     */
    onRequest(request, context, stateController) {

    }
}